import { DateSequencePipe } from './date-sequence.pipe';

describe('DateSequencePipe', () => {
  it('create an instance', () => {
    const pipe = new DateSequencePipe();
    expect(pipe).toBeTruthy();
  });
});
